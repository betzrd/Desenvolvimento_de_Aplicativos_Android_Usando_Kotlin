// Seção 06
fun main() {

    var teste: String = "Hello World"

    println(teste)

    // comentário em uma linha
    /* comentário em várias linhas */
    /**
     * Comentário Documentação*/

    /* Variável: funcionalidade que é capaz de armazenar um valor que pode ser usado durante a execução de um programa;
    * Regras:
    * 1) uma String é capaz de armazenar um texto; é um conjunto de caracteres;
    * 2) var (variable);
    * 3) uma variável tem 2 coisas obrigatórias: nome e tipo;
    * 4) no exemplo a cima o nome da variável é "teste";
    * 5) entre aspas duplas está sempre um texto;
    * 6) não pode declarar a variável 2x;
    * 7) as variáveis são únicas no programa;
    * 8) nomeclatura das variáveis: não pode espaços em branco; caracteres especiais; não é recomendado acentos, palavras reservadas não podem definir uma variável {ex.: var; fun}
    * 9) val: não altera - var: altera;
    * 10) número de { que estão abrindo deve ser igual ao número de } que estão fechando;
    * 11) indentação: são as linhas auxilixam a ver quem esta dentro de quem;
    * 12) ctrl + alt + l: para formatar o código;
    * 13) os arquivos não podem ter o mesmo nome;
    * TIPOS DE DADOS:
    * 1) Boolean: falso (false) ou verdadeiro (true);
    * 2) Character: usa com aspas simples e tem apenas uma letra, mas sem restrição;
    * 3) Double e Float: únicos tipos de dados que conseguem aceitar ponto flutuante [ex.: 0,2];
    * 4) Long, Integer, Short e Byte: aceitam apenas números inteiros [sem casa decimal];
    * 5) Diferença entre Double e Float: Double tem muito mais precisão [usado em cálculos de grande magnitude que são matemáticos, financeiros, estatísticos] e mais poder de armazenamento;
    * 6) computador entende que quando tem ponto é entendido com Double; para ele aceitar apenas colocar um "F" na frente do número quebrado;
    * 7) Byte < Short < Integer < Long;
    * 8) se não expecificar e colocar apenas a atribuição, o computar vai definir o tipo da variável;
    * 9) se tiver uma variável que não tem o seu tipo definido explicitamente definido e o valor atribuído cabe dentro do inteiro, o tipo da variável é inteiro. Se não couber, o tipo da variável é Long;
    * 10) String pode crescer indefinidamente, não existe um padrão para a String no armazenamento;
    * 11) String varia o seu armazenamento de acordo com o conteúdo;
    * 12) JVM se encarrega durante a execução de definir o tamanho de um Boolean;
    * 13) quando colocar (Double.) entra nas características do Double;
    * */
}